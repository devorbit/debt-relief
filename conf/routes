
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           com.experian.debtrelief.controller.HomeController.index
# An example controller showing how to use dependency injection
# GET     /count                      com.experian.debtrelief.controllers.CountController.count
# An example controller showing how to write asynchronous code
# GET     /message                    com.experian.debtrelief.controllers.AsyncController.message

# Map static resources from the /public folder to the /assets URL path
# GET     /assets/*file               com.experian.debtrelief.controllers.Assets.versioned(path="/public", file: Asset)

POST    /consumer/registration      com.experian.debtrelief.controller.ConsumerController.registerConsumer()

